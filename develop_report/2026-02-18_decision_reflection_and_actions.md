# 개발 보고서: 의사결정 반영 및 후속 조치

- 작성일: 2026-02-18
- 전달 대상: 기획 스레드
- 참조 의사결정:
  1. 포함 범위: `app/`, `db/`, `tests/`, `src/`
  2. Python 환경: `.venv`
  3. 키 운영: 기존 키 사용 가능 여부 질의
  4. 커밋 전략: 문서/코드 분리 커밋

## 1. 반영 완료 사항
1. Python 가상환경 표준을 `.venv`로 반영 완료
- 기존 `.venv`(Python 3.14)는 백업: `.venv_py314_backup_2026-02-18`
- 새 `.venv`를 Python 3.13으로 재생성
- 의존성 재설치 완료

2. 동작 검증 완료
- 실행: `.venv/bin/python --version`
- 결과: `Python 3.13.5`
- 실행: `.venv/bin/pytest -q`
- 결과: `15 passed`

3. 산출물 포함 범위 확정 반영
- 이번 주 코드 산출물 기준 디렉터리: `app/`, `db/`, `tests/`, `src/`

4. 커밋 전략 반영
- 이후 커밋은 `문서 변경`과 `코드 변경`을 분리하는 원칙으로 진행 예정

## 2. 현재 상태 메모
1. 워킹트리에는 위 범위 외에도 보고서/레퍼런스 디렉터리가 다수 존재
2. 실제 커밋 수행 전, 분리 커밋 기준으로 파일 집합을 명시적으로 나눠 staging 필요

## 3. 보안 관련 의사결정 필요 (추가 질문)
1. 질의하신 "기존 키를 일단 사용"은 기술적으로 가능하지만, 현재 프로젝트 원칙(D0 rotate)과는 충돌함
2. 권고안:
- 개발 진행은 임시로 기존 키를 쓰더라도,
- **2026-02-19(목) 내 `service_role` rotate 완료**를 마감으로 확정 필요

3. 확인 요청
- A안: 오늘부터 기존 키 임시 사용 + 2026-02-19 내 rotate 강제
- B안: 지금 즉시 rotate 후 개발 진행

## 4. 다음 실행 대기 항목
1. 보안안(A/B) 확정
2. 분리 커밋 실제 수행(문서 커밋 1개 + 코드 커밋 1개)
